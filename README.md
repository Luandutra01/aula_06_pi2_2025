# CRUD de Alunos – Node.js + Express + MongoDB + Front-end

Projeto simples utilizando:
- Front-end: HTML, CSS e JavaScript  
- Backend: Node.js + Express  
- Banco: MongoDB local  
- Live Server (GoLive) para rodar o front-end  
---
## 🚀 1. Instalação
necessário colocar o endereço do banco de dados em server.js: mongoose.connect("mongodb+srv://(inserir endereço)", {

Abra o terminal dentro da pasta do projeto e execute:

```sh
npm init -y
npm install express mongoose cors

node server.js (executar em uma janela e executar o goLive em outra)
